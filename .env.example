# API keys for each provider. Copy this file to `.env` and fill in the keys you have.
# Any provider whose key is missing will be unavailable in the model selector
# unless the key is entered manually in the sidebar UI at runtime.

GOOGLE_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
