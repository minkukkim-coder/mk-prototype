streamlit>=1.42.0
google-genai>=0.3.0
anthropic>=0.40.0
openai>=1.50.0
python-dotenv>=1.0.0
pyyaml>=6.0
# Required for Streamlit native OIDC auth (st.login / st.user)
Authlib>=1.3.2
# Persistent storage backend (per-user memory, chat history, custom prompts)
supabase>=2.7.0
